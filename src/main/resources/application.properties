server.port=${PORT:0}
spring.application.name: spring-cloud-user-api
eureka.client.serviceUrl.defaultZone: ${EUREKA_URI:http://eureka-server:8761/eureka}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false